package project

import (
	"context"
	"go.opentelemetry.io/collector/pdata/pmetric"
)

func processMetrics(
	ctx context.Context,
	pm pmetric.Metrics,
) (mt pmetric.Metrics, err error) {

	return mt, nil

}
